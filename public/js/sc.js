(function () {
    "use strict"

    // Configuration

    /**
     * GLobal configuration - frozen to prevent accidental mutation.
     * All timing values in miliseconfs unless specified
     * @constant {Readonly<object>}
     */

    const GITHUB = Object.freeze({
        USERNAME: 'hayaxxdev-bit.my.id',
        
    })
})